/* components/custom-checkbox/custom-checkbox.wxss */

.custom-checkbox-container {
  display: inline-block;
}

.custom-checkbox-box {
  display: flex;
  align-items: center;
}

.custom-checkbox-box.left {
  flex-direction: row-reverse;
}

.custom-checkbox-box.right {
  flex-direction: row;
}

.custom-checkbox {
  margin-left: 10rpx;
}

.content {
  font-size: 24rpx;
}

/* 复选框组件是公共组件 */
/* 以后需要在多个页面或需要在多个项目中进行使用 */
/* 所以需要先给复选框组件准备并设置一些默认样式 */
/* 如果在其他页面或其他项目中使用的时候，发现样式不符合产品需求 */
/* 可以对默认的复选框样式进行修改 */

/* 复选框默认样式设置步骤 */
/* 1. 需要给复选框设置默认样式 */
/* 需要先找到微信小程序给复选框提供的类名，通过小程序提供的类名才能对复选框样式进行修改 */
/* 需要先打开小程序开发文档，找到复选框文档，F12审查元素，进行查找 */

/* 复选框没有选中时默认的样式 */
/* 如果需要进行修改，需要设置 styleIsolation 为 shared */
/* shared: 能够修改其他页面、组件使用者、以及任何样式隔离为 shared 和 apply-shared 的组件*/
/* 这不是我们想要的结果，我们只希望修改当前组件的样式，此时可以添加命名空间，即命名细致化 */
.custom-checkbox .wx-checkbox-input {
  width: 24rpx !important;
  height: 24rpx !important;
  border-radius: 50% !important;
  border: 1px solid #fda007 !important;
  margin-top: -6rpx;
}

/* 复选框选中时默认的样式 */
.custom-checkbox .wx-checkbox-input-checked {
  background-color: #fda007 !important;

}

/* 复选框选中时 √ 样式 */
.custom-checkbox .wx-checkbox-input.wx-checkbox-input-checked:before {
  font-size: 22rpx;
  color: #fff;
}